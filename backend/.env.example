DATABASE_URL=postgresql://user:@17May2007__@host:port/dbname
SECRET_KEY=your-secret-key-here
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
```

#### **1.3 Create `backend/Procfile`** (for deployment)
```
web: uvicorn app.main:app --host 0.0.0.0 --port $PORT
```

#### **1.4 Create `backend/runtime.txt`**
```
python-3.11.0